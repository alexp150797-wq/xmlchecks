# Правила парсинга XML (все поля опциональны)
entry_tag: ModelFile
name_tag: FileName
checksum_tag: FileChecksum
format_tag: FileFormat
filter_format: IFC  # Можно несколько, через запятую (например: IFC,PFD)
